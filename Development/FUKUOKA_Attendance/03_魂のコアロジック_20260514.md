# 魂のコアロジック：プロンプトとソースコード

## 1. AIプロンプト（8大見本例）
AI（Gemini）の解析精度を担保するための核。以下の見本を必ず含めること。
1. 複数人の同時報告
2. 弁当の有無、部外者の除外
3. 出張・現場の判別
4. 複数日の合算報告
5. 「全員弁当」等の全体適用ルール

## 2. 最新のGASソースコード（抜粋）
```javascript
// 日付のズレを根絶するロジック
const parts = entry.日付.split('-'); // YYYY-MM-DD
const targetDate = new Date(parts[0], parts[1] - 1, parts[2], 0, 0, 0); // 時刻を0固定
const nowJST = Utilities.formatDate(new Date(), "Asia/Tokyo", "yyyy/MM/dd HH:mm:ss");

// 月別ファイルの自動生成ロジック
const fileName = "【実績ログ】" + monthLabel;
const folder = DriveApp.getFileById(MASTER_SS_ID).getParents().next();
const newSS = SpreadsheetApp.create(fileName);
folder.addFile(DriveApp.getFileById(newSS.getId()));
```

## 3. Python集計ロジック
- Google Sheets API経由でマスターからデータを取得。
- Excelテンプレートの指定セルに実績を流し込む。
